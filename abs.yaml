# dependence on linux sysroot, eg lib64/libc.so.6, don't do overlinking checks
build_parameters:
  - "--suppress-variables"
  - "--skip-existing"

upload_without_merge: True
upload_to_staging_channel_in_pr: False
channels: [cuda12_def]